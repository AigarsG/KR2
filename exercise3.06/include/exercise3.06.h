#ifndef EXERCISE3_06_H_LOADED
#define EXERCISE3_06_H_LOADED

/**
 * Converts n to characters in s
 * and pads with blanks on left if necessary to satisfy minimum width min_width.
 * Returns 0 on successful convertion, 1 on error
 */
int itoa(int n, char s[], size_t sz, size_t min_width);

#endif
